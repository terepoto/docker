version: "2"
services:
  node:
    image: "node:11-alpine"
    user: "node"
    working_dir: /home/node/app
    tty: true
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/home/node/app
    ports:
      - "8080:8080"
